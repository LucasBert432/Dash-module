import { css } from "@emotion/react";

const useStyles = () => {
  return {
    wrapper: css`
      display: flex;
      align-items: center;
      justify-content: center;
      margin-top: 72px;
      margin-bottom: 72px;
    `,
  };
};

export default useStyles;
