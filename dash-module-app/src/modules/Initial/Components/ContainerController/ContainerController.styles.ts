import { css } from "@emotion/react";

const useStyles = () => ({
  wrapper: css`
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
  `,
});

export default useStyles;
